import { type NextPage } from "next";

const Home: NextPage = () => {
  return (
    <main className="flex h-screen justify-center">
      <div className="flex h-full w-full bg-gradient-to-t from-slate-800 to-cyan-800"></div>
    </main>
  );
};

export default Home;
